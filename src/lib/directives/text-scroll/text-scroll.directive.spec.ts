import { TextScrollDirective } from './text-scroll.directive';

describe('TextScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new TextScrollDirective({} as any, {} as any);
    expect(directive).toBeTruthy();
  });
});
